package main

import (
	"fmt"
	"net/http"
	"net/url"
	"strings"
	"io"
)

func main() {
	// Let's try some requests to a mock or local server?
	// But we don't have the server running. We can just test the handler directly.
}
